<div class="content-header">Backup and Forward</div>

<div class="content">
    Backup and Forward are <code>music_data</code> records with discriminator values of <code>backup</code> and <code>forward</code>, respectively.
</div>

<div class="content">
    The structures are the same, except that forward includes staff and voice values.
</div>

<div class="content">
    The image below shows the <code>music_data</code> columns that forward uses.
</div>

<img class="table-layout-image" src="images/table_layout/forward.png" alt="forward"/>
